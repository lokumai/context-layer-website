# State and Auth Implementation Plan

> **Superseded by `2026-04-22-08-personas-and-fixtures.md`.** The early scaffolding originally described here (single-user auth, flag-based store, hardcoded-gating navbar) has been replaced by a three-persona model backed by `@context-layer/mocks`. Do **not** execute the tasks below as a separate pass — they would create conflicts with plan 08. This file is retained only to document the historical scope.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mock authentication, global playground state, and the universal Top Navbar.

**Status in plan 08:**

| Original task | Now lives in |
|---|---|
| Task 1 — Mock auth (single user) | Plan 08, Task 5 (three-persona credentials) |
| Task 2 — Zustand playground store | Plan 08, Task 7 (persona-scoped, persisted, mutations) |
| Task 3 — Top Navbar | Plan 08, Task 9 (persona badge + reset + state-derived gating) |

> **Source of Truth:** All technical and design decisions derive from `../../SEED.md`, `../../UI_UX.md`, and `../../DESIGN.md`. Read them before implementing plan 08.

**Tech Stack:** Next.js 15, Zustand (+ persist middleware), Auth.js v5 (NextAuth), Tailwind CSS v4.

---

### Notes for executors

- The feature branch `feature/personas-mock-data` contains early commits that implemented the single-user / flag-based versions of these tasks. Plan 08 Task 0 describes the cleanup path (drop the persona commit, keep the scaffolding commits). After that cleanup, plan 08 tasks produce the canonical auth, store, and navbar.
- Do not re-author `apps/web/auth.ts`, `apps/web/store/playground-store.ts`, or `apps/web/components/layout/top-navbar.tsx` from this plan. Use plan 08.
